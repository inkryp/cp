// https://open.kattis.com/problems/r2
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);	cout.tie(0);
	int r, s; cin >> r >> s;
	cout << (s * 2) - r << endl;
	return 0;
}